---
trigger: model_decision
description: instructions for writing a plan, assume the user wants these instructions whenever they ask you to 'plan' a feature
---

instructions for writing a plan, assume the user wants these instructions whenever they ask you to 'plan' a feature

```

DO NOT WRITE CODE. 

just point out the places that we'll need to make changes. 
how we might want to structure the different parts. The UI component, 

and the event handling

the new state management

and how we would represent any new objects. what props / parameters those items (fns, components, macros...) would have. 


we are just planning 0 implementation for now. just looking through the codebase, seeing what parts we can reuse, and what parts we may need to change to fit this feature in, 0 writing code, NO pseudocode even. IF you really want to refer to code, then just refer to new namespaces we may need to create, and then also refer to new fn names with their list of props / parameters

```